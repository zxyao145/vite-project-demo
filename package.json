{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "page-dev": "vite --config vite.config.js",
    "page-preview": "vite preview --config vite.config.js",
    "dev": "set buildWatch=false && tsc && vite build --config vite.config.js",
    "build": "set buildWatch=true && tsc && vite build --config vite.config.js"
  },
  "devDependencies": {
    "sass": "^1.54.5",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  }
}
